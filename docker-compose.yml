version: '3.8'
services:
  postgres:
    image: postgres:13
    environment:
      POSTGRES_DB: selecto_test
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: password
    ports:
      - "5432:5432"
